<%= form_for(@termdeposit) do |f| %>
  <% if @termdeposit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@termdeposit.errors.count, "error") %> prohibited this termdeposit from being saved:</h2>

      <ul>
      <% @termdeposit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="table table-hover table-responsive">

  <thead>
  <td><h2>Add Term Deposit</h2></td> 
    <tr>
      <th>Term Deposit Description</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= f.text_field :description %></td>
        <td>$<%= f.number_field :value %></td>
        <td><div class="Actions"><%= f.submit 'Create Term Deposit' %></td>
      </tr>
  </tbody>
</table> 

<% end %>