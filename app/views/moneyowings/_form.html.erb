<%= form_for(@moneyowing) do |f| %>
  <% if @moneyowing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moneyowing.errors.count, "error") %> prohibited this moneyowing from being saved:</h2>

      <ul>
      <% @moneyowing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="table table-hover table-responsive">

  <thead>
  <td><h2>Add Money Owing</h2></td> 
    <tr>
      <th>Money Owing Description</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= f.text_field :description %></td>
        <td>$<%= f.number_field :value %></td>
        <td><div class="actions"><%= f.submit %></td>
      </tr>
  </tbody>
</table>
<% end %>
