<p id="notice"><%= notice %></p>

<h1>Current Assets</h1>

<table class="table table-hover table-responsive">

  <thead>
  <td><h2>Vendor Finance: $<%= @vendorfinances.sum(:value) %></h2></td>
    <tr>
      <th>Tax Entity</th>
      <th>Prior Year</th>
      <th>Current YTD</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  </thead>

  <tbody>
    <% @vendorfinances.each do |vendorfinance| %>
      <tr>
        <td><%= vendorfinance.description %></td>
        <td><%= vendorfinance.value %></td>
        <td><%= vendorfinance.value %></td>
        <td><%= link_to 'View Detail', vendorfinance %></td>
      </tr>
    <% end %>
    <tr>
        <td><strong>Total</strong></td>
        <td> <strong>$<%= @vendorfinances.sum(:value) %></strong></td>
        <td><strong>$<%= @vendorfinances.sum(:value) %></strong></td>
        <td></td>
      </tr>
    <tr>
      <td><%= link_to 'Back', summary_path %> </td>
      <td></td>
      <td></td>
      <td><%= link_to '+ New Vendor Finance', new_vendorfinance_path %></td>
    </tr>
  </tbody>
</table>





