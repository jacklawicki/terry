<%= form_for(@vendorfinance) do |f| %>
  <% if @vendorfinance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vendorfinance.errors.count, "error") %> prohibited this vendorfinance from being saved:</h2>

      <ul>
      <% @vendorfinance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="table table-hover table-responsive">

  <thead>
  <td><h2>Add Term Deposit</h2></td> 
    <tr>
      <th>Term Deposit Description</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= f.text_field :description %></td>
        <td>$<%= f.number_field :value %></td>
        <td><div class="actions"><%= f.submit %></td>
      </tr>
  </tbody>
</table> 
<% end %>
