<p id="notice"><%= notice %></p>

<h1>Listing Entities</h1>

<table>
  <thead>
  <h2>Cash</h2>
    <tr>
      <th>Details</th>
      <th>Amount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
   
  </tbody>
</table>

<table>
  <thead>
  <h2>Money Owing</h2>
    <tr>
      <th>Details</th>
      <th>Amount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
   <% @entities.each do |entity, money_owing| %> 
      <tr>
        <td><%= entity.money_owing %></td>
        <td><%= entity.details %></td>
        <td><%= entity.amount %></td>
        <td><%= link_to 'Add Money Owing', money_path %></td>
        <td><%= link_to 'Show', entity %></td>
        <td><%= link_to 'Edit', edit_entity_path(entity) %></td>
        <td><%= link_to 'Destroy', entity, method: :delete, data: { confirm: 'Are you sure?' } %>
      </tr>
      
   <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Money', money_path %>
<%= link_to 'New Entity', new_entity_path %>
